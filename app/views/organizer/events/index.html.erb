<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <%# Header %>
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-8">
    <h1 class="text-3xl font-bold text-gray-900"><%= t("organizer.events.index.title") %></h1>
    <%= link_to new_organizer_event_path, class: "mt-4 sm:mt-0 inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition" do %>
      <svg class="h-5 w-5 mr-2 -ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
      </svg>
      <%= t("organizer.events.index.create_event") %>
    <% end %>
  </div>

  <%# Events Table %>
  <div class="bg-white rounded-lg shadow overflow-hidden">
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"><%= t("organizer.events.index.name") %></th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"><%= t("organizer.events.index.venue") %></th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"><%= t("organizer.events.index.status") %></th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"><%= t("organizer.events.index.starts_at") %></th>
            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"><%= t("organizer.events.index.tickets_sold") %></th>
            <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider"><%= t("organizer.events.index.actions") %></th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @events.each do |event| %>
            <tr class="hover:bg-gray-50 transition">
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="text-sm font-medium text-gray-900"><%= event.name %></div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                <%= event.venue&.name || t("organizer.events.index.no_venue") %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                  <% case event.status %>
                  <% when 'draft' %>
                    bg-gray-100 text-gray-800
                  <% when 'published' %>
                    bg-green-100 text-green-800
                  <% when 'cancelled' %>
                    bg-red-100 text-red-800
                  <% when 'completed' %>
                    bg-blue-100 text-blue-800
                  <% end %>
                ">
                  <%= t("statuses.#{event.status}") %>
                </span>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                <%= l(event.starts_at, format: :short) %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                <%= event.tickets_sold %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium space-x-2">
                <%= link_to t("organizer.events.index.view"), organizer_event_path(event), class: "text-indigo-600 hover:text-indigo-900" %>
                <%= link_to t("organizer.events.index.edit"), edit_organizer_event_path(event), class: "text-purple-600 hover:text-purple-900" %>
                <%= link_to t("organizer.events.index.delete"), organizer_event_path(event), data: { turbo_method: :delete, turbo_confirm: t("organizer.events.index.delete_confirm") }, class: "text-red-600 hover:text-red-900" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <% if @events.empty? %>
      <div class="px-6 py-12 text-center">
        <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
        </svg>
        <h3 class="mt-2 text-sm font-medium text-gray-900"><%= t("organizer.events.index.no_events") %></h3>
        <p class="mt-1 text-sm text-gray-500"><%= t("organizer.events.index.get_started") %></p>
        <div class="mt-6">
          <%= link_to new_organizer_event_path, class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700" do %>
            <svg class="h-5 w-5 mr-2 -ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
            </svg>
            <%= t("organizer.events.index.create_event") %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>

  <%# Pagination %>
  <% if @pagy && @pagy.pages > 1 %>
    <div class="mt-6 flex justify-center">
      <%== pagy_nav(@pagy) %>
    </div>
  <% end %>
</div>
