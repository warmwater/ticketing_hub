<div class="px-4 py-6 sm:px-6 lg:px-8">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Venues</h1>
    <%= link_to "New Venue", new_admin_venue_path, class: "inline-flex items-center px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition" %>
  </div>

  <div class="bg-white rounded-lg shadow overflow-hidden">
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">City</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Country</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Capacity</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @venues.each do |venue| %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-indigo-600">
                <%= link_to venue.name, admin_venue_path(venue), class: "hover:text-indigo-900" %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700"><%= venue.city %></td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700"><%= venue.country %></td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700"><%= number_with_delimiter(venue.capacity) %></td>
              <td class="px-6 py-4 whitespace-nowrap text-right text-sm space-x-2">
                <%= link_to "View", admin_venue_path(venue), class: "text-indigo-600 hover:text-indigo-900 font-medium" %>
                <%= link_to "Edit", edit_admin_venue_path(venue), class: "text-indigo-600 hover:text-indigo-900 font-medium" %>
                <%= link_to "Delete", admin_venue_path(venue), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this venue?" }, class: "text-red-600 hover:text-red-900 font-medium" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <div class="mt-4">
    <%== pagy_nav(@pagy) %>
  </div>
</div>
